# The private environment — recreated anytime from requirements.txt
.venv/
venv/

# Python's compiled cache files
__pycache__/
*.pyc

# Runtime output, not source code
data/*.db
logs/*.log
# ...but DO commit the served database (Option 1 bakes it into the deploy).
!data/consensus.db

# PyCharm's own local project settings
.idea/

.env